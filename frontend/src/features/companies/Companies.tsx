import { Box, Typography } from '@mui/material';

const Companies = () => {
  return (
    <Box>
      <Typography variant="h4">Empresas (Em desenvolvimento)</Typography>
    </Box>
  );
};

export default Companies;