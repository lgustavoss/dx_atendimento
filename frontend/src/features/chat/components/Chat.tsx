import { Box, Typography } from '@mui/material';

const Chat = () => {
  return (
    <Box>
      <Typography variant="h4">Chat (Em desenvolvimento)</Typography>
    </Box>
  );
};

export default Chat;