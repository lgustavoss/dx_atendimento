import { Box, Typography } from '@mui/material';

const Users = () => {
  return (
    <Box>
      <Typography variant="h4">Usuários (Em desenvolvimento)</Typography>
    </Box>
  );
};

export default Users;