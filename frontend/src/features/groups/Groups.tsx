import { Box, Typography } from '@mui/material';

const Groups = () => {
  return (
    <Box>
      <Typography variant="h4">Grupos (Em desenvolvimento)</Typography>
    </Box>
  );
};

export default Groups;