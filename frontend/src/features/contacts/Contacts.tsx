import { Box, Typography } from '@mui/material';

const Contacts = () => {
  return (
    <Box>
      <Typography variant="h4">Contatos (Em desenvolvimento)</Typography>
    </Box>
  );
};

export default Contacts;